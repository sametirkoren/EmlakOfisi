@model IEnumerable<Entities.Concrete.Admin>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<script>
    $(document).ready( function () {
        $('#table_id').DataTable(
            {
                "language": {
                    "url" : "//cdn.datatables.net/plug-ins/9dcbecd42ad/i18n/Turkish.json"
                }
            }
        );
    } );
</script>

<h2>Admin Kayıtları</h2>

<a class="btn btn-outline-dark mt-2 mb-2" href="/Admin/Register/">Yeni Admin Ekle</a>


<table class="table table-striped table-sm" id="table_id">
    <thead>
    <tr>
        <th>Admin Id</th>
        <th>Admin Kullanıcı Adı</th>
        <th>İşlemler</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Id</td>
            <td>@item.UserName</td>
            <td><a class="btn btn-primary btn-sm" href="/Admin/Edit/@item.Id">Düzenle</a> | <a class="btn btn-danger btn-sm" href="/Admin/Delete/@item.Id">Sil</a></td>
        </tr>
    }

    </tbody>
</table>