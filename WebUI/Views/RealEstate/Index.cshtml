@model IEnumerable<Entities.Concrete.RealEstate>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<script>
    $(document).ready( function () {
        $('#table_id').DataTable(
            {
                "language": {
                    "url" : "//cdn.datatables.net/plug-ins/9dcbecd42ad/i18n/Turkish.json"
                }
            }
        );
    } );
</script>

<h2>Emlakçı Kayıtları</h2>

<a class="btn btn-outline-dark mt-2 mb-2" href="/RealEstate/Create/">Yeni Emlakçı Ekle</a>


<table class="table table-striped table-sm" id="table_id">
    <thead>
    <tr>
        <th>Emlakçı Id</th>
        <th>Firma Adı</th>
        <th>Kullanıcı Adı</th>
        <th>Mail Adresi</th>
        <th>Adresi</th>
        
        <th>İşlemler</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Id</td>
            <td>@item.CompanyName</td>
            <td>@item.UserName</td>
            <td>@item.Mail</td>
            <td>@item.Address</td>
            <td><a class="btn btn-primary btn-sm" href="/RealEstate/Edit/@item.Id">Düzenle</a> | <a class="btn btn-danger btn-sm" href="/RealState/Delete/@item.Id">Sil</a></td>
        </tr>
    }

    </tbody>
</table>


