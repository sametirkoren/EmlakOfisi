@model IEnumerable<Entities.Concrete.Heating>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<script>
    $(document).ready( function () {
        $('#table_id').DataTable(
            {
                "language": {
                    "url" : "//cdn.datatables.net/plug-ins/9dcbecd42ad/i18n/Turkish.json"
                }
            }
        );
    } );
</script>

<h2>Isınma Tipi Kayıtları</h2>

<a class="btn btn-outline-dark mt-2 mb-2" href="/Heating/Create/">Yeni Isınma Tipi Ekle</a>


<table class="table table-striped table-sm" id="table_id">
    <thead>
    <tr>
        <th>Isınma Tipi Id</th>
        <th>Isınma Tipi Adı</th>
        <th>İşlemler</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Id</td>
            <td>@item.Name</td>
            <td><a class="btn btn-primary btn-sm" href="/Heating/Edit/@item.Id">Düzenle</a> | <a class="btn btn-danger btn-sm" href="/Heating/Delete/@item.Id">Sil</a></td>
        </tr>
    }

    </tbody>
</table>